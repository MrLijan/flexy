export default {
  props: {
    active: {
      type: Boolean,
      required: false,
      default: false,
    },

    disabled: {
      type: Boolean,
      required: false,
      default: false,
    },

    icon: {
      type: Boolean,
      default: false,
      required: false,
    },
  },
};
