export default {
  props: {
    disabled: {
      type: Boolean,
      required: false,
      default: false,
    },

    icon: {
      type: String,
      default: 'add-circle',
      required: false,
    },
  },
};
